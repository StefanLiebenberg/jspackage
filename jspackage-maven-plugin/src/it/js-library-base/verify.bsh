import java.io.*;

File file = new File( basedir, "target/it-js-library-base-" + version + ".jar");
if ( !file.isFile() ){

        throw new FileNotFoundException( "Could not find generated JAR: " + file );
        }
